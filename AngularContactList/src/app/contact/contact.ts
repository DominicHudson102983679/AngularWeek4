export class Contact {
    name: string;
    title: string;
    address: string;

    constructor(name: string, title: string, address: string) {
        this.name = name;
        this.title = title;
        this.address = address;
    }
}
